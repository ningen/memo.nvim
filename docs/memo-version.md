# MemoVersion

## Purpose

`MemoVersion` prints plugin and Neovim version metadata into a scratch buffer.

## Why This Helps

The project now has optional integrations and many commands.
Version metadata makes bug reports and support notes easier to compare.

## Usage

```vim
:MemoVersion
```
